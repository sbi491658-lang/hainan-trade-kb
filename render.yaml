services:
  - type: web
    name: hainan-trade-kb
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app_v5.py
    envVars:
      - key: PORT
        value: 10000
